/**
 * @desc controller for all projects
 */
const projectController = {
  /**
   * @desc creates a project
   * @param {object} req
   * @param {object} res
   */
  async createProject(req, res) {
    return true;
  },

  /**
   * @desc update the details of a project
   * @param {object} req
   * @param {object} res
   */
  async updateProject(req, res) {
    return true;
  },

  /**
   * @desc retrieves all project
   * @param {object} req
   * @param {object} res
   */
  async getAllProjects(req, res) {
    return true;
  },

  /**
   * @desc deletes a project
   * @param {object} req
   * @param {object} res
   */
  async deleteProject(req, res) {
    return true;
  }
};

module.exports = projectController;
